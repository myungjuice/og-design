const {chromium}=require(process.env.PLAYWRIGHT_PATH||'playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:1100}});await p.goto('http://127.0.0.1:4173/design-system/canvas/');await p.evaluate(()=>document.fonts.ready);await p.locator('[data-board="help"]').click();
assert(await p.locator('.og-heading-info,.og-help-trigger').evaluateAll(es=>es.every(e=>e.classList.contains('og-info-button'))),'info buttons share component');
const gap=await p.evaluate(()=>{const e=document.querySelector('#help-tooltip-specimen .specimen-overlay'),a=e.getBoundingClientRect(),i=e.parentElement.querySelector('.og-info-button .material-icons').getBoundingClientRect(),s=new DOMMatrix(getComputedStyle(document.querySelector('#world')).transform).a;return {y:(a.top-i.bottom)/s,x:(a.left-i.left)/s}});assert(Math.abs(gap.y-8)<1);assert(Math.abs(gap.x)<1);
for(const sel of ['.og-time-value','.og-attachment-status','.og-attachment-actions button'])assert(await p.locator(sel).evaluateAll(es=>es.every(e=>getComputedStyle(e).lineHeight!=='normal')),sel);
const read=()=>p.locator('#quantity-min button').first().evaluate(e=>{const s=getComputedStyle(e);return {font:s.fontFamily,opacity:s.opacity}});
const before=await read();await p.evaluate(()=>document.querySelector('link[href^="canvas.css"]').disabled=true);assert.deepEqual(await read(),before,'component independent of canvas reset');await p.evaluate(()=>document.querySelector('link[href^="canvas.css"]').disabled=false);
await p.locator('[data-board="sheet"]').click();await p.locator('[data-board-prompt="sheet"]').click();await p.waitForFunction(()=>!document.querySelector('#board-prompt-copy').disabled);const value=await p.locator('#board-prompt-text').inputValue();assert(value.includes('.og-info-button'));assert(value.includes('font-size:var(--app-label-size)'));await p.keyboard.press('Escape');
assert(await p.locator('.og-heading-info[aria-expanded="true"]').evaluateAll(es=>es.every(e=>getComputedStyle(e).color===getComputedStyle(document.querySelector('.og-help-trigger[aria-expanded="true"]')).color)));
console.log('PASS: shared controls, 8px anchored tooltip, type line heights, standalone style, handoff CSS');
}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
